modules:
  dns_vip_udp:
    prober: dns
    timeout: 3s
    dns:
      preferred_ip_protocol: ip4
      transport_protocol: udp
      query_name: "google.com"
      query_type: "A"
      recursion_desired: true
      valid_rcodes:
        - NOERROR
