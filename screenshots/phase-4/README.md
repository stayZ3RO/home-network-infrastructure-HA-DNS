# Phase 4 Screenshot Assets

This folder contains **working placeholder SVGs** for the Phase 4 documentation.

These files are intentionally committed so the markdown image links in the documentation resolve on GitHub instead of breaking. Replace each placeholder with the real screenshot while keeping the same filename.

## Files

* `docker-wsl-working.svg`
* `monitoring-stack-running.svg`
* `grafana-node-health-dashboard.svg`
* `grafana-dns-failover-dashboard.svg`
* `alert-lifecycle.svg`
* `failover-monitoring.svg`
* `monitoring-config-backup.svg`

## Replacement Guidance

When you swap in the real screenshots:

1. keep the same filename
2. use `.png` only if you also update the markdown links
3. crop out unnecessary UI clutter where possible
4. keep terminal screenshots readable at GitHub width
