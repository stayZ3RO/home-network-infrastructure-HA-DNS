# Phase 5 Screenshot Assets

This folder contains **working placeholder SVGs** for the Phase 5 documentation.

These files are intentionally committed so the markdown image links in the documentation resolve on GitHub instead of breaking. Replace each placeholder with the real screenshot while keeping the same filename.

## Files

* `tailscale-admin-console.svg`
* `tailscale-devices-connected.svg`
* `tailscale-ping-success.svg`
* `ssh-ashpi-1-over-tailscale.svg`
* `ssh-ashpi-2-over-tailscale.svg`
* `offsite-ssh-success.svg`

## Replacement Guidance

When you swap in the real screenshots:

1. keep the same filename
2. use `.png` only if you also update the markdown links
3. blur or crop any sensitive public IPs or account details
4. keep the SSH proof readable and focused on the successful connection
